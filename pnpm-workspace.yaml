packages:
  - 'apps/*'
  - 'packages/*'

# pnpm blocks postinstall scripts unless explicitly allowed. esbuild needs its
# postinstall to place the platform binary Vite depends on.
allowBuilds:
  esbuild: true
